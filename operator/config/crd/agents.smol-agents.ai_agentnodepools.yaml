# Generated from kubebuilder markers in operator/api/v1/agentnodepool_types.go.
# Repo-wide `make -C operator manifests` currently fails on an unrelated marker
# in pkg/agentmodel/v1/storage.go; to regenerate JUST this group's CRDs, scope
# controller-gen to the package:
#   controller-gen crd:crdVersions=v1 paths=./operator/api/v1 \
#     output:crd:dir=operator/config/crd
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: agentnodepools.agents.smol-agents.ai
spec:
  group: agents.smol-agents.ai
  names:
    kind: AgentNodePool
    listKind: AgentNodePoolList
    plural: agentnodepools
    shortNames:
    - anp
    singular: agentnodepool
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.isolation
      name: Isolation
      type: string
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.arch
      name: Arch
      type: string
    - jsonPath: .spec.bootstrap.mode
      name: Bootstrap
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.capacityAvailable
      name: Capacity
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: AgentNodePool is the cluster-scoped node-shape CR.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AgentNodePoolSpec declares a kata-capable node shape. The operator
              compiles each AgentNodePool into a Karpenter NodePool + EC2NodeClass
              (dedicated, tainted) and the workload builder binds agents whose
              sandbox runtimeClass matches `isolation` onto it. Node→cluster join
              is supplied by the existing Karpenter deployment; this CR only adds
              the kata/devmapper layer. See docs/design/agent-platform.md.

              Implements R-PROV-1, R-PROV-3, R-PROV-4.
            properties:
              arch:
                default: arm64
                description: Arch constrains the node architecture.
                enum:
                - arm64
                - amd64
                type: string
              bootstrap:
                description: Bootstrap selects how the kata layer is delivered to
                  the node.
                properties:
                  amiSelector:
                    description: AMISelector selects the kata-ready image for Mode=PrebakedAMI.
                    items:
                      description: AMISelectorTerm mirrors a Karpenter EC2NodeClass
                        amiSelectorTerm.
                      properties:
                        alias:
                          type: string
                        id:
                          type: string
                        tags:
                          additionalProperties:
                            type: string
                          type: object
                      type: object
                    type: array
                  distro:
                    description: Distro picks which hardened recipe to append for
                      Mode=UserData.
                    enum:
                    - al2023
                    - bottlerocket
                    - flatcar
                    - fedora-coreos
                    type: string
                  mode:
                    description: |-
                      Mode is PrebakedAMI (kata baked onto the join-capable base image;
                      userData only creates the thin-pool) or UserData (kata recipe
                      appended after the existing join snippet, installed at boot).
                    enum:
                    - PrebakedAMI
                    - UserData
                    type: string
                required:
                - mode
                type: object
              capacityType:
                default:
                - on-demand
                description: CapacityType is the Karpenter capacity-type allow-list.
                items:
                  type: string
                type: array
              disruption:
                description: Disruption maps to the Karpenter NodePool disruption
                  policy.
                properties:
                  consolidateAfter:
                    default: Never
                    description: ConsolidateAfter is a Karpenter duration string or
                      "Never".
                    type: string
                  consolidationPolicy:
                    default: WhenEmpty
                    enum:
                    - WhenEmpty
                    - WhenEmptyOrUnderutilized
                    type: string
                type: object
              instanceFamilies:
                description: |-
                  InstanceFamilies Karpenter may choose from. For kata isolations these
                  MUST be bare-metal families — only `*.metal` instances expose /dev/kvm.
                items:
                  type: string
                type: array
              isolation:
                description: |-
                  Isolation this pool provides. Drives the KVM/metal requirement and
                  the runtimeClass→pool match performed by the workload builder.
                enum:
                - kata-fc
                - kata-clh
                - gvisor
                - runc
                type: string
              provider:
                default: Karpenter
                description: |-
                  Provider selects the node-provisioning backend. Karpenter creates
                  in-cluster NodePool/EC2NodeClass objects; ClusterAutoscaler emits the
                  node-group spec (CAS discovery tags + kata userData) as a ConfigMap
                  for an externally-managed ASG.
                enum:
                - Karpenter
                - ClusterAutoscaler
                type: string
              limits:
                description: Limits caps total resources Karpenter will provision
                  for this pool.
                properties:
                  cpu:
                    type: string
                  memory:
                    type: string
                type: object
              minNodes:
                default: 0
                description: |-
                  MinNodes keeps a warm floor (0 = pure on-demand provisioning).
                  Reserved for P2 serverless cold-start mitigation.
                format: int32
                minimum: 0
                type: integer
              thinPool:
                description: ThinPool configures the devmapper thin-pool kata-fc needs.
                properties:
                  backing:
                    default: instance-store
                    enum:
                    - instance-store
                    - ebs
                    type: string
                  dataSize:
                    default: 50Gi
                    type: string
                  metaSize:
                    default: 5Gi
                    type: string
                type: object
            required:
            - bootstrap
            - isolation
            type: object
          status:
            description: AgentNodePoolStatus is the observed state.
            properties:
              capacityAvailable:
                description: CapacityAvailable mirrors whether Karpenter reports launchable
                  capacity.
                type: boolean
              conditions:
                description: Conditions carries the aggregate "Ready" and "KarpenterSynced".
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              nodeClassName:
                type: string
              nodePoolName:
                description: NodePoolName / NodeClassName are the owned Karpenter
                  object names.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last generation reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is one of Pending, Reconciling, Ready, Degraded.
                enum:
                - Pending
                - Reconciling
                - Ready
                - Degraded
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
