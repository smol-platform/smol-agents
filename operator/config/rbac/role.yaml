# Generated by hand to mirror the verbs the controllers actually call.
# Implements R-OP-SEC-1: no `*` verbs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: knative-agents-operator
rules:
  # CR access — control-plane CRs.
  - apiGroups: [agents.stigen.ai]
    resources: [knativeagents, knativeagentplatforms]
    verbs: [get, list, watch, update, patch]
  - apiGroups: [agents.stigen.ai]
    resources: [knativeagents/status, knativeagentplatforms/status]
    verbs: [get, update, patch]
  - apiGroups: [agents.stigen.ai]
    resources: [knativeagents/finalizers, knativeagentplatforms/finalizers]
    verbs: [update]

  # CR access — runtime CRs (Agent, AgentRun, AgentNetwork, etc).
  - apiGroups: [runtime.agents.stigen.ai]
    resources: [agents, tools, modelproviders, agentruns, agentsessions, agentpolicies, agentnetworks]
    verbs: [get, list, watch, update, patch]
  - apiGroups: [runtime.agents.stigen.ai]
    resources:
      - agents/status
      - agentruns/status
      - agentsessions/status
      - agentpolicies/status
      - agentnetworks/status
    verbs: [get, update, patch]

  # AgentRun reconciler creates Pods for each run.
  - apiGroups: [""]
    resources: [pods]
    verbs: [get, list, watch, create, update, patch, delete]

  # AgentNetwork resolves broker secrets to fail fast.
  - apiGroups: [""]
    resources: [secrets]
    verbs: [get, list, watch]

  # Identity feature
  - apiGroups: [""]
    resources: [configmaps, serviceaccounts]
    verbs: [get, list, watch, create, update, patch, delete]
  - apiGroups: [spire.spiffe.io]
    resources: [clusterspiffeids]
    verbs: [get, list, watch, create, update, patch, delete]

  # Sandbox feature
  - apiGroups: [node.k8s.io]
    resources: [runtimeclasses]
    verbs: [get, list, watch, create, update, patch, delete]

  # Workload feature (Deployment, StatefulSet, Knative Service)
  - apiGroups: [apps]
    resources: [deployments, statefulsets, daemonsets]
    verbs: [get, list, watch, create, update, patch, delete]
  - apiGroups: [serving.knative.dev]
    resources: [services]
    verbs: [get, list, watch, create, update, patch, delete]

  # eBPF loader RBAC subjects
  - apiGroups: [rbac.authorization.k8s.io]
    resources: [clusterroles, clusterrolebindings]
    verbs: [get, list, watch, create, update, patch, delete]

  # Events
  - apiGroups: [""]
    resources: [events]
    verbs: [create, patch]

  # Leader election
  - apiGroups: [coordination.k8s.io]
    resources: [leases]
    verbs: [get, list, watch, create, update, patch, delete]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: knative-agents-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: knative-agents-operator
subjects:
  - kind: ServiceAccount
    name: knative-agents-operator
    namespace: knative-agents-system
