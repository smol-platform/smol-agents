# Bottlerocket user-data — minimal SSM-Online smoke.
#
# THREE architectural approaches were investigated end-to-end for
# extending the Bottlerocket smoke to the same full health gate as
# AL2023/Ubuntu/Flatcar; ALL THREE are blocked by Bottlerocket
# design constraints. See scripts/aws-l2/bottlerocket-bootstrap/
# README.md for the full investigation report.
#
# The Bottlerocket smoke validates only AMI launch + SSM
# reachability. The other three distros pass the full health gate.

[settings.host-containers.admin]
enabled = false
